import { HideShowInfoDirective } from "./hide-show-info.directive";

describe("HideShowInfoDirective", () => {
  it("should create an instance", () => {
    const directive = new HideShowInfoDirective();
    expect(directive).toBeTruthy();
  });
});
